using UnityEngine;

/// <summary>
/// This is used for the physical items players can find
/// The useable versions that go in the inventory are handled by TemplateItem
/// </summary>
public class TemplateGroundItem : MonoBehaviour
{
    public TemplateItem thisItem;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
